export enum PaymentStatus {
  PENDING = 'pending',
  UNAUTHORIZED = 'unauthorized',
  PAID = 'paid',
  CANCELLED = 'cancelled',
}

export enum PaymentVendor {
  VNPAY = 'vnpay',
}

export enum PaymentType {
  POINT = 'point',
  MONEY = 'money',
}

export enum LocationType {
    ADMIN = 'admin',
    CUSTOMER = 'customer'
}

export enum LocationStatus {
    PENDING = 'pending',
    REJECTED = 'rejected',
    APPROVED = 'approved',
}

export enum LocationNFTStatus {
    PENDING = 'pending',
    REJECTED = 'rejected',
    APPROVED = 'approved',
    MINTED = 'minted'
}
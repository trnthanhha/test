export const APPOINTMENT_DEFAULT_SORT = 'name@asc';

export enum ENUM_APPOINTMENT_STATUS_CODE_ERROR {
    APPOINTMENT_NOT_FOUND_ERROR = 5501,
}

export enum ENUM_APPOINTMENT_MESSAGE {
    APPOINTMENT_CREATE = "create appointment",
    APPOINTMENT_UPDATE = "update appointment",
    APPOINTMENT_DELETE = "delete appointment",
}

export enum ENUM_APPOINTMENT_TYPE {
    KHAM_MOI = 'KHÁM MỚI',
    TAI_KHAM = 'TÁI KHÁM',
}
